import DocsHero from '../ui/DocsHero.jcr'
import PreviousNext from '../ui/PreviousNext.jcr'
import { shippedComponents, componentHref, previousNext } from '../nav-data.js'

const navLinks = previousNext('/components')
const catalog = shippedComponents.filter(
  (item) => !['ThemeScope', 'ThemeToggle', 'VisuallyHidden'].includes(item.name),
)

export <view>
  <article class="docs-page">
    <DocsHero
      :kicker=${'Components'}
      :title=${'Catalog'}
      :description=${'Browse every shipped primitive in @jacare/ui. Each card opens a page with a playground, live demos, Jacaré source, generated API from the .jcr contract, and previous/next navigation.'}
    />

    <div class="docs-card-grid">
      #for catalog as item (item.name)
        <a class="docs-card docs-panel" jacare-go=${componentHref(item.name)} href=${componentHref(item.name)}>
          <h3>${item.name}</h3>
          <p>${item.blurb}</p>
        </a>
      #end
    </div>

    <PreviousNext :previous=${navLinks.previous} :next=${navLinks.next} />
  </article>
</view>
