import { pulse } from '@jacare/core'
import Field from '@jacare/ui/components/Field.jcr'
import Switch from '@jacare/ui/components/Switch.jcr'
import Stack from '@jacare/ui/components/Stack.jcr'
import DatePickerPanel from '@jacare/ui/components/DatePickerPanel.jcr'
import DocsHero from '../../ui/DocsHero.jcr'
import Demo from '../../ui/Demo.jcr'
import DocsSection from '../../ui/DocsSection.jcr'
import PropsTable from '../../ui/PropsTable.jcr'
import PreviousNext from '../../ui/PreviousNext.jcr'
import CodeBlock from '../../ui/CodeBlock.jcr'
import AccessibilityChecklist from '../../ui/AccessibilityChecklist.jcr'
import { getContract } from '../../generated/contracts.js'
import { previousNext } from '../../nav-data.js'
import {
  basic,
  borderless,
  bounds,
  disabled,
  multiple,
  playgroundCode as buildPlaygroundCode,
  range,
} from '../../examples/date-picker-panel.js'

const contract = getContract('DatePickerPanel')
const navLinks = previousNext('/components/date-picker-panel')

const playValue = pulse('2026-07-15')
const playMin = pulse('')
const playMax = pulse('')
const playBorder = pulse(true)
const playDisabled = pulse(false)
const basicValue = pulse('2026-07-15')
const boundsValue = pulse('2026-07-10')
const borderlessValue = pulse('2026-07-15')
const disabledValue = pulse('2026-07-15')
const rangeValue = pulse(['2026-07-10', '2026-07-18'])
const multipleValue = pulse(['2026-07-08', '2026-07-15', '2026-07-22'])

const a11yItems = [
  'Each day button has a full date aria-label',
  'Disabled days (outside min/max) are not selectable',
  'Month and year selects remain keyboard accessible',
  'Range and multiple modes announce selection state in the hint',
  'Today action jumps to the current date when allowed',
]

function playgroundCode() {
  return buildPlaygroundCode({
    value: playValue(),
    min: playMin(),
    max: playMax(),
    border: playBorder(),
    disabled: playDisabled(),
  })
}

function formatList(values) {
  const list = Array.isArray(values) ? values : []
  return list.length ? list.join(', ') : 'None'
}

export <view>
  <article class="docs-page">
    <nav class="docs-breadcrumb" aria-label="Breadcrumb">
      <a jacare-go="/components" href="/components">Components</a>
      <span aria-hidden="true">/</span>
      <span>DatePickerPanel</span>
    </nav>

    <DocsHero
      :kicker=${'Pickers'}
      :title=${'DatePickerPanel'}
      :description=${'Inline calendar panel for a single ISO date, a date range, or multiple dates. Embed it in popovers, side panels, or next to other form fields when you do not need DatePicker chrome.'}
      :status=${'stable'}
    />

    <DocsSection :id=${'playground'} :title=${'Playground'}>
      <div class="docs-alert-play">
        <div class="docs-alert-play__controls docs-panel">
          <Field :label=${'Min (YYYY-MM-DD)'} bind-value=${playMin} />
          <Field :label=${'Max (YYYY-MM-DD)'} bind-value=${playMax} />
          <Stack :direction=${'row'} :gap=${'lg'} :wrap=${true}>
            <Switch :label=${'Border'} bind-checked=${playBorder} />
            <Switch :label=${'Disabled'} bind-checked=${playDisabled} />
          </Stack>
        </div>

        <div class="docs-alert-play__preview">
          <div class="docs-panel">
            <DatePickerPanel
              :min=${playMin}
              :max=${playMax}
              :border=${playBorder}
              :disabled=${playDisabled}
              bind-value=${playValue}
            />
          </div>
          <CodeBlock :code=${() => playgroundCode()} :filename=${'date-picker-panel-playground.jcr'} :language=${'jacare'} />
        </div>
      </div>
    </DocsSection>

    <Demo
      :id=${'basic'}
      :title=${'Basic'}
      :lead=${'Always-visible calendar with month/year selects and a Today action.'}
      :code=${basic}
      :filename=${'date-picker-panel-basic.jcr'}
    >
      <DatePickerPanel bind-value=${basicValue} />
    </Demo>

    <Demo
      :id=${'range'}
      :title=${'Range'}
      :lead=${'Set range and bind an array of two ISO dates. First click sets the start; second click completes the range.'}
      :code=${range}
      :filename=${'date-picker-panel-range.jcr'}
      :layout=${'stack'}
    >
      <Stack :gap=${'md'}>
        <DatePickerPanel :range=${true} bind-value=${rangeValue} />
        <p class="docs-lead">${() => `Selected: ${formatList(rangeValue())}`}</p>
      </Stack>
    </Demo>

    <Demo
      :id=${'multiple'}
      :title=${'Multiple'}
      :lead=${'Set multiple and bind an array of ISO dates. Tap again to deselect a day.'}
      :code=${multiple}
      :filename=${'date-picker-panel-multiple.jcr'}
      :layout=${'stack'}
    >
      <Stack :gap=${'md'}>
        <DatePickerPanel :multiple=${true} bind-value=${multipleValue} />
        <p class="docs-lead">${() => `Selected: ${formatList(multipleValue())}`}</p>
      </Stack>
    </Demo>

    <Demo
      :id=${'borderless'}
      :title=${'Borderless'}
      :lead=${'Remove the panel border when a surrounding surface already provides containment.'}
      :code=${borderless}
      :filename=${'date-picker-panel-borderless.jcr'}
    >
      <DatePickerPanel :border=${false} bind-value=${borderlessValue} />
    </Demo>

    <Demo
      :id=${'disabled'}
      :title=${'Disabled'}
      :lead=${'Navigation, selects, days, and the Today action become non-interactive together.'}
      :code=${disabled}
      :filename=${'date-picker-panel-disabled.jcr'}
    >
      <DatePickerPanel :disabled=${true} bind-value=${disabledValue} />
    </Demo>

    <Demo
      :id=${'bounds'}
      :title=${'Bounds'}
      :lead=${'Days outside min/max are disabled. Use this when embedding the panel in a constrained booking flow.'}
      :code=${bounds}
      :filename=${'date-picker-panel-bounds.jcr'}
    >
      <DatePickerPanel
        :min=${'2026-07-05'}
        :max=${'2026-07-25'}
        bind-value=${boundsValue}
      />
    </Demo>

    <DocsSection :id=${'a11y'} :title=${'Accessibility'}>
      <AccessibilityChecklist :items=${a11yItems} />
    </DocsSection>

    <DocsSection :id=${'api'} :title=${'API'}>
      <PropsTable :contract=${contract} />
    </DocsSection>

    <PreviousNext :previous=${navLinks.previous} :next=${navLinks.next} />
  </article>
</view>
